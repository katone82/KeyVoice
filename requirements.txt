# Libreria per acquisizione audio
PyAudio==0.2.13

# Librerie per Vosk ASR
vosk==0.3.45
numpy==1.25.2
scipy==1.11.3

# Libreria per fuzzy matching
rapidfuzz==3.14.1

# Librerie standard di utilità

# Libreria per beep cross-platform
simpleaudio

# Voice Activity Detection
webrtcvad

# MQTT: notifica di fine timer da Home Assistant
paho-mqtt

# Sintesi vocale Piper (opzionale, voce più naturale di
# espeak-ng — solo se command_feedback.tts_engine="piper" in
# config.json, vedi services/README.md). Se resti su espeak-ng
# non serve installarla.
# piper-tts
